<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="MantuGames.Views.Controls.RulesPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid x:Name="Overlay"
          BackgroundColor="#CC0B0E14"
          InputTransparent="False">

        <Border Background="{AppThemeBinding Light=#182136, Dark=#182136}"
                Stroke="{AppThemeBinding Light=#2EFFFFFF, Dark=#2EFFFFFF}"
                StrokeThickness="1"
                Padding="24"
                MaximumWidthRequest="340"
                HorizontalOptions="Center"
                VerticalOptions="Center">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="20" />
            </Border.StrokeShape>
            <VerticalStackLayout Spacing="16" HorizontalOptions="Center">
                <Label x:Name="TitleLabel"
                       Text="How to Play"
                       FontFamily="Orbitron"
                       FontSize="22"
                       FontAttributes="Bold"
                       TextColor="#E7ECF5"
                       HorizontalOptions="Center" />
                <ScrollView MaximumHeightRequest="320">
                    <Label x:Name="BodyLabel"
                           FontFamily="Inter"
                           FontSize="15"
                           LineHeight="1.5"
                           TextColor="#9AA7BD"
                           HorizontalOptions="Start" />
                </ScrollView>
                <Border BackgroundColor="#FF9F1C"
                        StrokeThickness="0"
                        Padding="32,10"
                        HorizontalOptions="Center"
                        MinimumWidthRequest="140">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="14" />
                    </Border.StrokeShape>
                    <Label Text="Got it!"
                           FontFamily="Orbitron"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#160D02"
                           HorizontalOptions="Center">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnDismiss" />
                        </Label.GestureRecognizers>
                    </Label>
                </Border>
            </VerticalStackLayout>
        </Border>
    </Grid>

</ContentView>
